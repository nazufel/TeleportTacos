syntax = "proto3";

package pb;

option go_package = "./pb";

message MenuItem {
    bytes id = 1; 
    string name = 2;
    string description = 3;
    float price = 4;
}

message EmptyRequest {}

service TacoService {
    rpc GetMenu(EmptyRequest) returns (MenuItem) {}
}